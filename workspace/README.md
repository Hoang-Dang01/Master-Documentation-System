---
ownership: project-runtime
status: canonical
source: user-and-workflow-output
safe_to_modify: limited
canonical_target: workspace/projects/active/<project-id>/
---

# Project workspace

Runtime project context and artifacts. This content is not global MDS policy.

The canonical project registry is `projects/index.yaml`. Each active project
uses `projects/active/<project-id>/`; the current project is `edumeet`.
