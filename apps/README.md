---
ownership: mds
status: canonical
source: internal
safe_to_modify: true
---

# Applications

Delivery surfaces for MDS. Application shells may depend on `packages/` and
consume `mds-core/`; they do not own business or governance rules.

Current application: `desktop/`.
