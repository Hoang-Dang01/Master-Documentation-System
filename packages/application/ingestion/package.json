{
  "name": "@mds/document-ingestion",
  "version": "0.1.0",
  "private": true,
  "description": "Local document import, normalization, preservation, and draft requirement extraction",
  "main": "dist/index.js",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "mammoth": "^1.12.0"
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "typescript": "^7.0.2"
  }
}
