---
ownership: mds
status: canonical
source: internal
safe_to_modify: scoped
canonical_target:
  - ../roles/role-model.md
  - ../roles/role-registry.yaml
update_strategy: extend only through an approved governed workflow
---

# Runtime environment

Production is the deployed operating environment and a source of operational
evidence. It is not a professional role or an MDS approval authority.

This folder is a boundary marker only. It does not create deployment,
production-operation, or runtime integration capabilities.
